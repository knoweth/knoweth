import styled from "styled-components";
/**
 * A container element that attempts to make its contents look like a piece of
 * paper from Google Docs.
 */
export default styled.div`
  box-shadow: 0 1px 3px 1px rgba(60, 64, 67, 0.15);
  padding: 0.25in; // inches for maximum AP Bio realism
`;
